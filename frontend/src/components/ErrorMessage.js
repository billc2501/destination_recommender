function ErrorMessage() {

  return (
    <div class="alert alert-warning alert-dismissible fade show" role="alert">
        <strong>Form Submission</strong> Please fill out at least one of the fields
    </div>
  );
}

export default ErrorMessage;